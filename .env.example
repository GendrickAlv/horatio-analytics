# PostgreSQL connection string.
# Matches the credentials in docker-compose.yml.
DATABASE_URL=postgresql://horatio:horatio@localhost:5432/horatio

# Optional: log level for pino (trace | debug | info | warn | error | fatal).
LOG_LEVEL=info
